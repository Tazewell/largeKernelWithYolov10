# Ultralytics YOLO 🚀, AGPL-3.0 license
# parent
# ├── ultralytics
# └── datasets
#     └── manhole  ← downloads here (100 MB)

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/manhole # dataset root dir
train: train/images
val: valid/images
test: test/images

# Classes
names:
  0: broken
  1: good
  2: lose
  3: uncovered

